import { {{properCase name}}Actions } from '../actions';

const initialState = [];

export function {{camelCase name}}Reducer({{camelCase name}}s = initialState, action) {
  switch (action.type) {

    default:
      return {{camelCase name}}s;

  }

}
