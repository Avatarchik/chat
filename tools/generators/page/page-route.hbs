import { RouterModule, Routes } from '@angular/router';
import { {{ properCase name }}Page } from '../pages/{{ dashCase name }}/{{ dashCase name }}.page';

const routes: Routes = [
  {path: '', component: {{ properCase name }}Page }
];

export const {{ camelCase name }}PageRoutes = RouterModule.forChild(routes);
